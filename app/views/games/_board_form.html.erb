<!-- this submits the coordinates to the controller
      - gets the variables from the locals hash
      - gets @game from /show, it wouldn't otherwise
      - have access to the instance variable
      - essentially, placement of buttons in relations to rows and columns-->
<%= form_for(game) do |f| %>
  <%= f.hidden_field :row, :value => row %>
  <%= f.hidden_field :column, :value => column %>
  <%= f.submit "#{row}, #{column}", class: "big-btn btn btn-large btn-primary" %>
<% end %>
